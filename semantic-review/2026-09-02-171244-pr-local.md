# Complete pricing inputs for the quotation product browser

The quotation product-browser now projects the complete product state consumed when its lean object is passed directly to `resolvePricing`. The previous blocker could make tier fallback and minimum-price validation use absent values. **Confirmed resolved:** every required identity, status, tier-rate, discount-target, and minimum-rate field is now available.

Watch for: None within the scope of the previously reported finding.

**Verdict**: APPROVED

## High-level view

The projection satisfies the full direct-object resolver contract: `_id` is included implicitly, while `status`, `productCode`, `mrp`, all six configured tier rates, `brand`, `category`, `subcategory`, and `minimumSellingRate` are selected explicitly.

<details>
<summary>Issues (0)</summary>

No remaining actionable issues in the re-reviewed finding.

</details>

<details>
<summary>Details</summary>

## Projected products now satisfy the complete resolver contract

`loadProduct` consumes `_id`, `status`, and `productCode`. `chooseBaseRate` can consume `mrp`, `dealerRate`, `wholesaleRate`, `retailRate`, `distributorRate`, `builderRate`, and `projectRate`; these cover every value allowed by `TIER_FIELDS` and every fallback candidate. Discount matching also has `_id`, `brand`, `subcategory`, and `category`, and minimum validation has `minimumSellingRate` rather than defaulting an omitted value to zero.

</details>

<details>
<summary>File map</summary>

- `routes/quotationRoutes.js` — projects the complete pricing input set for product-browser resolution.
- `services/pricingResolver.js` — defines the direct-product consumers verified by this re-review.

Full diff: `git diff main -- routes/quotationRoutes.js services/pricingResolver.js`

</details>
